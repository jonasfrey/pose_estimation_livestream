please create a small webapp that accesses the webcam of the computer and runs
live inference of a pose estimation model such as vitpose on it. then it
visualizes the pose and shows it on the live webcame as overlay.

use the smalles pose models available.

'deno task run' will install everything that is required and run the app
